{
"$schema": "https://json-schema.org/draft/2020-12/schema",
"$id": "schemas/context/get-context-response.schema.json",
"title": "Get Context Response",
"description": "Response schema for get_context, returning retrieved archive text as ordered blocks.",
"type": "object",
"additionalProperties": false,
"required": [
"success",
"result_count",
"results",
"meta",
"message"
],
"properties": {
"success": {
"type": "boolean",
"description": "Indicates whether the context retrieval request completed successfully."
},
"result_count": {
"type": "integer",
"minimum": 0,
"description": "Number of retrieved text blocks returned."
},
"results": {
"type": "array",
"description": "Ordered text blocks retrieved from archived content.",
"items": {
"type": "object",
"additionalProperties": false,
"required": [
"clean_text"
],
"properties": {
"clean_text": {
"type": "string",
"description": "Text content returned from the archive context retrieval workflow. Empty strings may occur when the source block is empty or structural."
}
}
}
},
"meta": {
"type": "object",
"additionalProperties": false,
"required": [
"source",
"scenario"
],
"properties": {
"source": {
"type": "string",
"minLength": 1,
"description": "Public source label for the archive context source."
},
"scenario": {
"type": "string",
"minLength": 1,
"description": "Workflow scenario label."
}
}
},
"message": {
"type": [
"string",
"null"
],
"description": "Optional human-readable response message."
}
}
}
